# Base URL for the backend API.
# Leave empty to use the Vite dev proxy (recommended for local dev).
# For production, set to your deployed API origin, e.g. https://api.example.com
VITE_API_BASE_URL=
